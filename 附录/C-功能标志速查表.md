# 附录 C：功能标志速查表

> 功能标志（Feature Flag）是 Claude Code 中实现编译时死代码消除（Dead Code Elimination）的核心机制。所有标志均在打包阶段通过编译器注入布尔值：当标志为 `false` 时，其守卫的代码分支会被整段移除，从而减小产物体积并保护未发布功能。
>
> **类型说明**：
> - **编译时**：标志在 Bun 打包阶段求值，结果为 `false` 的代码分支在产物中完全不存在
> - **编译时 + 运行时门控**：编译时为 `true`，但在运行时还需额外条件（如环境变量、服务端配置）才真正激活

---

## C.1 核心交互模式标志

| 功能标志 | 类型 | 作用描述 | 相关章节 |
|---|---|---|---|
| `PROACTIVE` | 编译时 | 主动模式。启用后智能体可在用户空闲时主动发起建议、自动执行后台任务 | 第 5 章 |
| `KAIROS` | 编译时 + 运行时门控 | 助手模式（原 Harbor）。面向 IDE 的通道式协作，含频道消息、会话恢复等完整功能集 | 第 6 章 |
| `KAIROS_BRIEF` | 编译时 | KAIROS 精简子集，仅启用会话恢复等基础能力，不包含完整通道功能 | 第 6 章 |
| `KAIROS_CHANNELS` | 编译时 + 运行时门控 | 独立于完整 KAIROS 的频道功能开关，允许仅启用频道消息通信 | 第 6 章 |
| `KAIROS_DREAM` | 编译时 | KAIROS Dream 模式，用于加载额外的技能集 | 第 6 章 |
| `KAIROS_GITHUB_WEBHOOKS` | 编译时 | GitHub Webhook 集成，在 KAIROS 模式下接收 GitHub 事件推送 | 第 6 章 |
| `KAIROS_PUSH_NOTIFICATION` | 编译时 | KAIROS 推送通知功能 | 第 6 章 |
| `BRIDGE_MODE` | 编译时 + 运行时门控 | IDE 桥接模式。启用后 REPL 通过桥接协议与 IDE（如 VS Code）通信，支持权限回调管道 | 第 7 章 |
| `COORDINATOR_MODE` | 编译时 + 运行时门控 | 协调器模式。在多智能体场景中充当协调者，分发任务并汇总结果 | 第 8 章 |
| `VOICE_MODE` | 编译时 | 语音模式。启用语音输入/输出、Push-to-Talk 按键绑定及语音集成模块 | 第 12 章 |
| `DAEMON` | 编译时 | 后台守护进程模式。允许 Claude Code 以守护进程方式长期运行 | 第 11 章 |
| `BUDDY` | 编译时 | 伴侣精灵（Companion Sprite）。在 REPL 界面中显示可交互的动画角色，提供情感化反馈 | 第 12 章 |

## C.2 智能体与子任务标志

| 功能标志 | 类型 | 作用描述 | 相关章节 |
|---|---|---|---|
| `FORK_SUBAGENT` | 编译时 | Fork 子智能体。允许在对话中通过 fork 机制创建独立子智能体处理子任务 | 第 8 章 |
| `AGENT_TRIGGERS` | 编译时 | 智能体触发器。启用定时任务调度（cron）、后台周期性执行的智能体行为 | 第 9 章 |
| `AGENT_TRIGGERS_REMOTE` | 编译时 | 远程智能体触发器。支持远程托管的定时触发器执行 | 第 9 章 |
| `ULTRAPLAN` | 编译时 | 超级规划模式。提供交互式规划对话框，允许用户审查和修改复杂任务的执行计划 | 第 5 章 |
| `VERIFICATION_AGENT` | 编译时 | 验证智能体。在任务完成后自动启动验证流程 | 第 8 章 |
| `BUILTIN_EXPLORE_PLAN_AGENTS` | 编译时 | 内置探索-规划智能体。提供预装的探索和规划类型的子智能体 | 第 8 章 |
| `AGENT_MEMORY_SNAPSHOT` | 编译时 | 智能体内存快照。支持对智能体状态进行快照和恢复 | 第 10 章 |
| `WORKFLOW_SCRIPTS` | 编译时 | 工作流脚本。启用本地工作流任务处理器，支持自动化脚本编排 | 第 9 章 |
| `TEMPLATES` | 编译时 | 模板系统。启用作业分类器（Job Classifier），用于识别和路由不同类型的用户请求 | 第 9 章 |

## C.3 上下文管理与压缩标志

| 功能标志 | 类型 | 作用描述 | 相关章节 |
|---|---|---|---|
| `REACTIVE_COMPACT` | 编译时 + 运行时门控 | 响应式压缩。在 Token 接近阈值时自动触发上下文压缩，而非等待用户确认 | 第 4 章 |
| `CONTEXT_COLLAPSE` | 编译时 | 上下文折叠。提供比传统压缩更激进的上下文缩减策略，包含专门的折叠 UI 和恢复机制 | 第 4 章 |
| `CACHED_MICROCOMPACT` | 编译时 | 缓存式微压缩。在微压缩过程中维护提示缓存边界，避免缓存失效导致的额外开销 | 第 4 章 |
| `HISTORY_SNIP` | 编译时 | 历史裁剪（Snip Compact）。对已处理的对话历史进行智能裁剪，保留关键信息的同时大幅缩减 token 占用 | 第 4 章 |
| `COMPACTION_REMINDERS` | 编译时 | 压缩提醒。在压缩过程中向用户展示提醒信息 | 第 4 章 |
| `PROMPT_CACHE_BREAK_DETECTION` | 编译时 | 提示缓存断裂检测。在压缩操作中检测并报告提示缓存边界的断裂情况 | 第 4 章 |
| `TOKEN_BUDGET` | 编译时 | Token 预算管理器。跟踪和可视化 Token 使用预算，提供预算超限预警 | 第 4 章 |
| `COMMIT_ATTRIBUTION` | 编译时 | 提交归属。在压缩后为代码提交添加压缩前后的上下文归属标记 | 第 4 章 |

## C.4 权限与安全标志

| 功能标志 | 类型 | 作用描述 | 相关章节 |
|---|---|---|---|
| `TRANSCRIPT_CLASSIFIER` | 编译时 | 转录分类器。基于对话内容自动判断权限模式（包括 auto 模式），替代纯手动权限切换 | 第 3 章 |
| `BASH_CLASSIFIER` | 编译时 | Bash 分类器。对 Bash 命令进行安全性分类，高置信度的安全命令可自动放行 | 第 3 章 |
| `HARD_FAIL` | 编译时 | 硬失败模式。在关键错误时直接终止而非降级处理 | 第 3 章 |
| `NATIVE_CLIENT_ATTESTATION` | 编译时 | 原生客户端认证。启用平台原生的客户端身份验证机制 | 第 3 章 |
| `ANTI_DISTILLATION_CC` | 编译时 | 反蒸馏保护。防止模型输出被用于模型蒸馏攻击 | 第 3 章 |

## C.5 工具与技能标志

| 功能标志 | 类型 | 作用描述 | 相关章节 |
|---|---|---|---|
| `MONITOR_TOOL` | 编译时 | 监控工具。在 Bash 工具执行后台任务时提供监控能力 | 第 7 章 |
| `WEB_BROWSER_TOOL` | 编译时 | 网页浏览器工具。启用内置浏览器面板，支持网页浏览和内容提取 | 第 7 章 |
| `MCP_SKILLS` | 编译时 | MCP 技能发现。允许从 MCP 服务器动态发现和加载技能 | 第 7 章 |
| `EXPERIMENTAL_SKILL_SEARCH` | 编译时 | 实验性技能搜索。启用基于语义的技能索引和搜索能力 | 第 7 章 |
| `SKILL_IMPROVEMENT` | 编译时 | 技能改进。支持对已安装技能进行自动优化和迭代 | 第 7 章 |
| `RUN_SKILL_GENERATOR` | 编译时 | 技能生成器运行器。支持动态生成新技能 | 第 7 章 |
| `BUILDING_CLAUDE_APPS` | 编译时 | Claude 应用构建模式。加载用于构建 Claude 应用的专用技能集 | 第 7 章 |
| `REVIEW_ARTIFACT` | 编译时 | 审阅工件。加载代码审阅相关技能 | 第 7 章 |
| `HOOK_PROMPTS` | 编译时 | 钩子提示词。允许钩子（Hook）注入自定义提示词到对话流中 | 第 9 章 |
| `CONNECTOR_TEXT` | 编译时 | 连接器文本块。支持在消息流中渲染特殊的连接器文本块类型 | 第 7 章 |
| `UDS_INBOX` | 编译时 | Unix Domain Socket 收件箱。通过 UDS 接收来自其他进程的消息 | 第 7 章 |
| `MCP_RICH_OUTPUT` | 编译时 | MCP 富输出。允许 MCP 工具返回结构化的富媒体内容 | 第 7 章 |
| `TREE_SITTER_BASH` | 编译时 | Tree-sitter Bash 解析。使用 Tree-sitter 对 Bash 命令进行精确的 AST 解析 | 第 3 章 |
| `TREE_SITTER_BASH_SHADOW` | 编译时 + 运行时门控 | Tree-sitter Bash 影子模式。在现有解析器旁并行运行 Tree-sitter 进行结果对比验证 | 第 3 章 |

## C.6 会话与持久化标志

| 功能标志 | 类型 | 作用描述 | 相关章节 |
|---|---|---|---|
| `BG_SESSIONS` | 编译时 | 后台会话。支持在后台维持独立的会话实例，允许长时间运行的任务脱离前台执行 | 第 11 章 |
| `AWAY_SUMMARY` | 编译时 | 离开摘要。当用户离开后返回时，自动生成离开期间的对话摘要 | 第 11 章 |
| `FILE_PERSISTENCE` | 编译时 | 文件持久化。启用会话级别的文件级持久化追踪 | 第 11 章 |
| `NEW_INIT` | 编译时 | 新初始化流程。使用改进后的会话初始化逻辑 | 第 11 章 |

## C.7 记忆与知识管理标志

| 功能标志 | 类型 | 作用描述 | 相关章节 |
|---|---|---|---|
| `TEAMMEM` | 编译时 + 运行时门控 | 团队记忆。启用团队级别的共享记忆文件系统，支持团队知识库的读写和同步 | 第 10 章 |
| `EXTRACT_MEMORIES` | 编译时 | 记忆提取。在会话结束时自动从对话中提取可复用的知识片段并写入记忆文件 | 第 10 章 |
| `LODESTONE` | 编译时 | 磁石（Lodestone）。启用增强型的记忆检索和匹配机制 | 第 10 章 |
| `MEMORY_SHAPE_TELEMETRY` | 编译时 | 记忆形态遥测。收集记忆文件的形态和使用情况的匿名遥测数据 | 第 10 章 |

## C.8 远程与连接标志

| 功能标志 | 类型 | 作用描述 | 相关章节 |
|---|---|---|---|
| `SSH_REMOTE` | 编译时 | SSH 远程模式。支持通过 SSH 连接到远程机器并运行 Claude Code | 第 11 章 |
| `DIRECT_CONNECT` | 编译时 | 直连模式。支持跳过代理直接连接到 Anthropic API | 第 11 章 |
| `CHICAGO_MCP` | 编译时 | Chicago MCP。启用特定的 MCP 服务器配置和计算机使用（Computer Use）集成 | 第 7 章 |
| `CCR_AUTO_CONNECT` | 编译时 | CCR 自动连接。自动建立与 CCR（Claude Code Remote）服务的连接 | 第 11 章 |
| `CCR_MIRROR` | 编译时 | CCR 镜像。支持会话状态在本地与远程之间的双向镜像 | 第 11 章 |
| `CCR_REMOTE_SETUP` | 编译时 | CCR 远程设置。启用远程环境的一键配置流程 | 第 11 章 |
| `SELF_HOSTED_RUNNER` | 编译时 | 自托管运行器。支持在自托管基础设施上运行智能体 | 第 11 章 |
| `BYOC_ENVIRONMENT_RUNNER` | 编译时 | BYOC 环境运行器。支持 "Bring Your Own Cloud" 环境中的智能体执行 | 第 11 章 |

## C.9 UI 与界面标志

| 功能标志 | 类型 | 作用描述 | 相关章节 |
|---|---|---|---|
| `MESSAGE_ACTIONS` | 编译时 + 运行时门控 | 消息操作。在消息上启用上下文操作按钮（如复制、重新生成等） | 第 12 章 |
| `TERMINAL_PANEL` | 编译时 + 运行时门控 | 终端面板。在全屏布局中启用独立的终端面板（快捷键 Meta+J） | 第 12 章 |
| `QUICK_SEARCH` | 编译时 | 快速搜索。启用对话内快速搜索功能 | 第 12 章 |
| `HISTORY_PICKER` | 编译时 | 历史选择器。提供可视化的对话历史浏览和切换界面 | 第 12 章 |
| `AUTO_THEME` | 编译时 + 运行时门控 | 自动主题。根据系统偏好自动切换明暗主题 | 第 12 章 |
| `STREAMLINED_OUTPUT` | 编译时 | 精简输出。减少界面中的冗余视觉元素，提供更紧凑的输出样式 | 第 12 章 |
| `NATIVE_CLIPBOARD_IMAGE` | 编译时 | 原生剪贴板图片。支持从系统剪贴板直接粘贴图片到对话中 | 第 12 章 |

## C.10 设置同步标志

| 功能标志 | 类型 | 作用描述 | 相关章节 |
|---|---|---|---|
| `UPLOAD_USER_SETTINGS` | 编译时 | 上传用户设置。将本地用户设置同步到云端 | 第 10 章 |
| `DOWNLOAD_USER_SETTINGS` | 编译时 + 运行时门控 | 下载用户设置。从云端拉取并应用用户设置到本地环境 | 第 10 章 |

## C.11 遥测与诊断标志

| 功能标志 | 类型 | 作用描述 | 相关章节 |
|---|---|---|---|
| `COWORKER_TYPE_TELEMETRY` | 编译时 | 协作者类型遥测。收集和上报协作者（如 IDE、终端）的类型信息 | 第 13 章 |
| `ENHANCED_TELEMETRY_BETA` | 编译时 | 增强遥测 Beta。启用扩展的匿名使用遥测数据收集 | 第 13 章 |
| `PERFETTO_TRACING` | 编译时 | Perfetto 追踪。集成 Chrome Perfetto 追踪框架，用于性能分析和时序可视化 | 第 13 章 |
| `SHOT_STATS` | 编译时 | 快照统计。收集和展示每次 API 调用的详细统计信息 | 第 13 章 |
| `SLOW_OPERATION_LOGGING` | 编译时 | 慢操作日志。记录执行时间超过阈值的操作以辅助性能诊断 | 第 13 章 |
| `ABLATION_BASELINE` | 编译时 | 消融基线。在 A/B 实验中作为基线对照组，用于评估新功能的影响 | 第 13 章 |

## C.12 基础设施与构建标志

| 功能标志 | 类型 | 作用描述 | 相关章节 |
|---|---|---|---|
| `UNATTENDED_RETRY` | 编译时 | 无人值守重试。在 API 调用失败时自动重试，无需用户干预 | 第 13 章 |
| `IS_LIBC_GLIBC` | 编译时 | 检测目标平台的 libc 是否为 glibc 实现，用于二进制兼容性判断 | 附录 |
| `IS_LIBC_MUSL` | 编译时 | 检测目标平台的 libc 是否为 musl 实现（如 Alpine Linux），用于二进制兼容性判断 | 附录 |
| `POWERSHELL_AUTO_MODE` | 编译时 | PowerShell 自动模式。为 Windows PowerShell 环境提供专用的自动化权限配置 | 附录 |
| `ALLOW_TEST_VERSIONS` | 编译时 | 允许测试版本。在版本检查中允许接受预发布/测试版本号 | 附录 |
| `SKIP_DETECTION_WHEN_AUTOUPDATES_DISABLED` | 编译时 | 自动更新禁用时跳过检测。当自动更新已被显式禁用时，跳过版本检测逻辑以减少启动延迟 | 附录 |
| `DUMP_SYSTEM_PROMPT` | 编译时 | 导出系统提示词。启用后将完整的系统提示词输出到日志或文件，用于调试 | 附录 |
| `OVERFLOW_TEST_TOOL` | 编译时 | 溢出测试工具。提供专用的测试工具用于验证上下文溢出处理逻辑 | 附录 |
| `ULTRATHINK` | 编译时 | 深度思考模式。启用扩展思考（Extended Thinking）能力 | 第 5 章 |
| `TORCH` | 编译时 | Torch 模式。实验性的增强推理功能 | 第 5 章 |

---

## C.13 标志使用统计

| 分类 | 数量 |
|---|---|
| 核心交互模式 | 12 |
| 智能体与子任务 | 9 |
| 上下文管理与压缩 | 8 |
| 权限与安全 | 5 |
| 工具与技能 | 14 |
| 会话与持久化 | 4 |
| 记忆与知识管理 | 4 |
| 远程与连接 | 8 |
| UI 与界面 | 7 |
| 设置同步 | 2 |
| 遥测与诊断 | 6 |
| 基础设施与构建 | 10 |
| **合计** | **89** |

> **注**：以上标志列表基于系统架构分析整理，随版本迭代可能有所增减。功能标志的具体启用方式由构建配置决定，部分标志还需配合运行时门控条件（如特定的激活检测函数）才能真正激活对应功能。
